{% if items|length > 0 %}
    <div class="col-sm-12 text-center">
        <ul class="list-inline list-footer">
            {% for item in items -%}
                <li>
                    <a href="{{ item.pageUrl }}">
                        {{ item.label|trans }}
                    </a>
                </li>
            {% endfor -%}
        </ul>
    </div>
{% endif %}
